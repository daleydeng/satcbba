data {
  satellites {
    mode = "random"
    random {
        count = 2
    }
  }

  tasks {
    mode = "random"
    random {
        count = 20
    }
  }
}

dds {
  domain_id = 0
  max_iterations = 100
}

viz {
  enabled = true
  enable_map = true
  show_task_info = true
  show_path_time = true
  image_size = List(1920, 960)
  font_size = 24.0
  line_thickness = 4
}

algo {
  method = "cbba"
  cbba {
    max_tasks_per_agent = 0
  }
}

output_config {
  dir = "examples/ex2/results"
  use_timestamp = true
  timestamp_fmt = "%Y-%m-%d_%H-%M-%S"
}
