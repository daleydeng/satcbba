data {
  satellites {
    mode = "random"
    random {
        count = 5
    }
  }

  tasks {
    mode = "random"
    random {
        count = 20
    }
  }
}

dds {
  domain_id = 0
  num_agents = 5
  max_iterations = 100
}

viz {
  enable_map = true
  show_task_info = true
  show_path_time = true
}

algo {
  method = "cbba"
  cbba {
    max_tasks_per_agent = 0
  }
}

output_config {
  dir = "examples/ex2/results"
  use_timestamp = true
  timestamp_fmt = "%Y-%m-%d_%H-%M-%S"
}
